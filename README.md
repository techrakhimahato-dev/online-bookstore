## Setup Instructions
1. Clone the repository
2. Navigate to `bookstore-backend` and run `npm install`
3. Navigate to `bookstore-frontend` and run `npm install`
4. Create your `.env` variables and start the development servers.
